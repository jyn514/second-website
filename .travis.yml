language: ruby

# minimize startup times
os: linux
sudo: false

# don't require sudo
addons:
  apt:
    packages:
    - linklint

before_install:
    gem install bundle

install:
  - bundle install

script:
  - "scripts/pre-commit"

