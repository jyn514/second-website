// string literals:
// L!
// F
// c

// impl+167
void error () {
  setTerminalRed();

}

bool compare(string, self) {
  string literal = "3PI";
  // Kali_Share_password__Password_impl
  add(literal, __Kali_Share_password_strLit1);
}

int main(int argc, char* argv[]) {
  if (argc <= 1) return 0;
  if (argc > 1) throw exception();
  if (!compare(string(argv[1]), password)) error();
}
